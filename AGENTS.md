# AGENTS.md

## Cursor Cloud specific instructions

### Repository state

This repository (`word_combo_tool`) is currently an empty project with only a `README.md`. The README describes it as a tool that "combines words to create scrabble collages."

### Available system runtimes

- **Python**: 3.12.3 (`python3`), with `pip` 24.0
- **Node.js**: v22.22.2, with `npm` 10.9.7

### Development environment

Since there is no application code, dependencies, or configuration yet, the update script is a no-op. Once code is added to the repository, update the update script (via `SetupVmEnvironment`) and this file accordingly.

### Running the project

No build, lint, test, or run commands exist yet. Once the project has source code:
- Update this section with the relevant commands
- Add an appropriate update script for dependency installation
